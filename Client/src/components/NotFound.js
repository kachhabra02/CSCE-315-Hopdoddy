import React from 'react'

/**
 * NotFound component renders a message indicating that the requested page or path does not exist.
 * Typically used as a fallback for undefined routes in a React application.
 */
function NotFound() {
  return (
    <p>Whoops, path not identified!</p>
  )
}

export default NotFound